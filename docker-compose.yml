services:
  postgres:
    image: postgres:16-alpine
    container_name: gamereason-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: gamereason
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - gamereason_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d gamereason']
      interval: 5s
      timeout: 5s
      retries: 10

  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    container_name: gamereason-server
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/gamereason?schema=public
      PORT: 3001
      AUTH_TOKEN_SECRET: ${AUTH_TOKEN_SECRET:-gamereason-dev-secret}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ['CMD-SHELL', 'wget -qO- http://127.0.0.1:3001/api/health || exit 1']
      interval: 10s
      timeout: 5s
      retries: 10

  client:
    build:
      context: .
      dockerfile: apps/client/Dockerfile
    container_name: gamereason-client
    restart: unless-stopped
    environment:
      APP_PROTOCOL: ${APP_PROTOCOL:-http}
      APP_DOMAIN: ${APP_DOMAIN:-localhost}
    depends_on:
      server:
        condition: service_healthy
    ports:
      - '80:80'
      - '443:443'
    volumes:
      - caddy_data:/data
      - caddy_config:/config

volumes:
  gamereason_postgres_data:
  caddy_data:
  caddy_config:
